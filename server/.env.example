# Backend Environment Variables

# Server Configuration
PORT=4000
NODE_ENV=development

# Database Configuration
# For local MongoDB: mongodb://localhost:27017/your-database-name
# For MongoDB Atlas: mongodb+srv://username:password@cluster.mongodb.net/your-database-name
MONGODB_URL=mongodb://localhost:27017/edtech-platform

# JWT Secret (Generate a strong random string - minimum 32 characters)
# You can generate one using: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
JWT_SECRET=your-super-secret-jwt-key-min-32-characters

# Cloudinary Configuration (for media storage)
CLOUD_NAME=your-cloudinary-cloud-name
API_KEY=your-cloudinary-api-key
API_SECRET=your-cloudinary-api-secret
FOLDER_NAME=YourProjectName

# Razorpay Configuration (for payment processing)
# Start with TEST keys, switch to LIVE keys only for production
RAZORPAY_KEY=your-razorpay-key-id
RAZORPAY_SECRET=your-razorpay-secret

# Email Configuration (SMTP)
MAIL_HOST=smtp.gmail.com
MAIL_USER=your-email@gmail.com
MAIL_PASS=your-app-specific-password

# Frontend URL (for CORS and email links)
# For Development: http://localhost:3000
# For Production: Your deployed frontend URL (e.g., https://your-app.netlify.app)
FRONTEND_URL=http://localhost:3000

# Support Email (displayed in email templates)
SUPPORT_EMAIL=support@example.com
